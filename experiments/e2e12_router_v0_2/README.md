# Router V0.2 → Online E2E12 Frozen Validation

Formal frozen evaluation. Router V0.2 and all existing retrieval, generation, citation, prompt, model and dataset assets are read-only. New outputs are confined to this directory.

The set was frozen before inference. Live Tavily Search/Extract bypassed prior caches. Generation and judging used the existing local Qwen model; scores are provisional proxies, not human validation. The first host command timed out after four completed cases; the runner resumed the remaining eight without reissuing completed requests.
