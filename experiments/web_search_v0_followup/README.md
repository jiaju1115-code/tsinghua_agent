# Academic Retrieval V0.1

Isolated follow-up to the frozen Web Search V0. Run `python run_v0_1_evaluation.py --phase dry`, then `--phase full-router`, then `--phase shadow`. The local Tavily key is read from the V0 `.env` only; it is never logged or copied here.
