# Training data staging

No training data has been placed here.

Every future dataset must include an immutable manifest with source/license, split, content hash, PII review, deduplication status, and an explicit check against `model_selection/heldout/heldout_index.json` and all Evaluation/Gold identifiers.
