from __future__ import annotations

import json
from datetime import date
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
OUT = ROOT / "experiments" / "runtime_v1_architecture_audit"

modules = [
    {"id":"kb_v1","name":"Core Knowledge Base V1","version":"KNOWLEDGE_BASE_V1","status":"FROZEN_PRODUCTION_READY","path":"data/03_knowledge_base/v1","runtime_entry":"src.retrieval_v1.DenseRetrieverV1","production_role":"frozen retrieval corpus","notes":"Formal frozen Core KB; legacy frozen paths preserved."},
    {"id":"frozen_bundle_v1_1","name":"Frozen Bundle V1.1 approved candidate","version":"FROZEN_BUNDLE_V1.1","status":"FROZEN_PRODUCTION_READY","path":"experiments/frozen_bundle_v1_1_candidate","runtime_entry":"approved portability reference","production_role":"recommended portability reference","notes":"Approved active reference, but not yet wired into the original DenseRetrieverV1 loader."},
    {"id":"dense_retriever_v1","name":"Dense Retriever V1","version":"RAG_RETRIEVAL_V1","status":"FROZEN_PRODUCTION_READY","path":"src/retrieval_v1/adapter.py","runtime_entry":"DenseRetrieverV1.retrieve(query, case_id)","production_role":"current orchestrator retriever","notes":"Frozen contract; current working-tree execution is BLOCKED by bundle raw hash verification."},
    {"id":"router_v0_2","name":"Router V0.2","version":"ROUTER_V0.2","status":"EXPERIMENTAL","path":"experiments/router_v0_2/src/router_v0_2.py","runtime_entry":"route(query)","production_role":"none","notes":"Evaluated experiment; no import/wiring into e2e_orchestrator_v1."},
    {"id":"dynamic_retriever","name":"Dynamic/BM25/Hybrid Retriever family","version":"DYNAMIC_RETRIEVER_V0.x","status":"EXPERIMENTAL","path":"experiments/dynamic_retriever_v0; experiments/dynamic_retriever_v0_2; experiments/core_dynamic_shadow_v1","runtime_entry":"experiment-specific","production_role":"none","notes":"Candidate and shadow-only; not the main production retriever."},
    {"id":"dynamic_campus","name":"Dynamic Campus Candidate V1","version":"DYNAMIC_CAMPUS_V1","status":"EVALUATION_ONLY","path":"data/04_kb_expansion_candidate/dynamic_campus_v1","runtime_entry":"staging/candidate loaders","production_role":"none","notes":"Candidate-only corpus; never merged into Core KB V1."},
    {"id":"evidence_v1","name":"Evidence Sufficiency V1","version":"EVIDENCE_SUFFICIENCY_V1","status":"FROZEN_PRODUCTION_READY","path":"src/evidence_sufficiency_v1","runtime_entry":"evaluate_evidence(query, case_id, retrieval_result)","production_role":"orchestrator stage","notes":"Deterministic contract layer; current direct caller is UnifiedE2EOrchestratorV1."},
    {"id":"citation_v1","name":"Citation Support V1","version":"CITATION_SUPPORT_V1","status":"FROZEN_PRODUCTION_READY","path":"src/citation_support_v1","runtime_entry":"build_support_package(...) ","production_role":"orchestrator stage","notes":"Maps required points to support units and source metadata."},
    {"id":"answer_v1","name":"Answer Generation V1","version":"ANSWER_GENERATION_V1","status":"FROZEN_PRODUCTION_READY","path":"src/answer_generation_v1","runtime_entry":"generate_answer(...) ","production_role":"orchestrator stage","notes":"Uses local frozen Qwen GGUF adapter when model_adapter is absent; no external API."},
    {"id":"orchestrator_v1","name":"Unified E2E Orchestrator V1","version":"E2E_ORCHESTRATOR_V1","status":"PRODUCTION_CANDIDATE","path":"src/e2e_orchestrator_v1/runtime.py","runtime_entry":"run_e2e(query, case_id)","production_role":"complete internal chain","notes":"Real internal wiring exists, but no user-facing caller and current default retriever verification is blocked."},
    {"id":"user_runtime","name":"Unified user-facing runtime","version":None,"status":"BLOCKED","path":None,"runtime_entry":None,"production_role":"none","notes":"No current CLI, API, chat loop, Streamlit, FastAPI, or Gradio entrypoint was found for the frozen QA chain."},
    {"id":"shadow_evaluations","name":"Dynamic/Core shadow evaluations","version":"SHADOW_V0/V1","status":"EVALUATION_ONLY","path":"evaluation/dynamic_retrieval_shadow_v0; evaluation/core_dynamic_e2e_shadow_v1; evaluation/core_dynamic_e2e_runtime_shadow_v1","runtime_entry":"evaluation runners","production_role":"none","notes":"Proxy and blocked runtime evidence; not user-facing production runtime."},
]

wiring = [
    {"edge":"Router → Retriever","status":"NOT_WIRED","evidence":"No current import/call from experiments/router_v0_2 into src/e2e_orchestrator_v1; orchestrator directly instantiates DenseRetrieverV1."},
    {"edge":"Retriever → Evidence","status":"WIRED_BUT_RUNTIME_BLOCKED","evidence":"UnifiedE2EOrchestratorV1.run_e2e calls evaluate_evidence after retrieval; default DenseRetrieverV1 currently fails bundle verification before producing output."},
    {"edge":"Evidence → Citation","status":"WIRED","evidence":"run_e2e passes retrieval and evidence results to build_support_package with frozen contract validation."},
    {"edge":"Citation → Answer","status":"WIRED","evidence":"run_e2e passes support package to generate_answer and enforces status propagation/no-model-call behavior on BLOCKED."},
    {"edge":"Answer → user runtime","status":"NOT_WIRED","evidence":"run_e2e returns a structured result to its caller; no current user-facing caller/renderer exists."},
]

inventory = {
    "audit_name":"Current System Consolidation / Runtime V1 Architecture Audit",
    "audit_version":"RUNTIME_ARCHITECTURE_AUDIT_V1",
    "generated_on":str(date.today()),
    "scope":"read-only architecture and wiring audit; no production runtime changes",
    "current_actual_runtime":["caller (direct Python only)","src.e2e_orchestrator_v1.run_e2e","src.retrieval_v1.DenseRetrieverV1","src.evidence_sufficiency_v1.evaluate_evidence","src.citation_support_v1.build_support_package","src.answer_generation_v1.generate_answer","structured final answer object"],
    "recommended_demo_runtime":["new demo CLI/API adapter (next task)","approved FROZEN_BUNDLE_V1.1 portability-aware retriever adapter","UnifiedE2EOrchestratorV1.run_e2e","Evidence Sufficiency V1","Citation Support V1","Answer Generation V1","rendered final answer"],
    "modules":modules,
    "wiring_audit":wiring,
    "duplicate_ambiguous_runtime_inventory":[
        {"family":"Router V0/V0.1/V0.2 and web-search routers","paths":["experiments/router_v0_2","experiments/web_search_v0","experiments/web_search_v0_followup","experiments/web_search_v0_1"],"recommendation":"retain as experiment/history; do not import into current runtime until a router contract is approved"},
        {"family":"Dense, BM25, dynamic and hybrid retrievers","paths":["src/retrieval_v1","experiments/dynamic_retriever_v0","experiments/dynamic_retriever_v0_2","experiments/core_dynamic_shadow_v1"],"recommendation":"keep Dense V1 as main chain; keep dynamic family evaluation-only"},
        {"family":"Evidence/Citation/Answer historical versions","paths":["experiments/evidence_sufficiency_v0.*","evaluation/citation/v1/v2","evaluation/answer_generation/v0","src/*_v1"],"recommendation":"src V1 is current internal chain; historical versions remain non-runtime"},
        {"family":"Proxy versus actual shadow E2E","paths":["evaluation/core_dynamic_e2e_shadow_v1","evaluation/core_dynamic_e2e_runtime_shadow_v1"],"recommendation":"label proxy and blocked actual results separately; neither is user-facing runtime"},
    ],
    "hybrid_decision":"KEEP_DENSE_V1_FOR_NOW",
    "hybrid_reason":"Dynamic/Hybrid has no approved production wiring or gold-regression evidence demonstrating superiority; it is not connected to frozen Evidence/Citation/Answer contracts.",
    "integration_gaps":{"P0":["create one user-facing demo entrypoint calling run_e2e","wire approved V1.1 portability-aware bundle loading so the default retriever passes integrity verification"],"P1":["perform actual Frozen Runtime E2E through the repaired loader","define and evaluate a Router→Retriever contract before any routing is enabled"],"P2":["re-evaluate Dynamic/Hybrid against the frozen gold set and promote only on evidence-backed improvement","add observability and user-facing rendering around the structured result"]},
    "answer_generation_audit":{"input":"Frozen Citation Support V1 package","model":"local Qwen Qwen2.5 1.5B GGUF adapter; no external API","caller":"UnifiedE2EOrchestratorV1 only","guardrails":"status propagation, provenance validation, prompt-injection refusal and no-model-call on blocked support","gap":"no user-facing renderer/caller; current runtime is blocked upstream at retriever integrity verification"},
    "agent_runtime_audit":{"agent_loop":"none found","tool_use":"none in current frozen chain","planning_or_memory":"none found","recommendation":"keep demo as deterministic orchestrator call; do not introduce agent autonomy in Runtime V1 integration"},
    "frozen_integrity":{"core_frozen_paths":"PASS - no production/frozen files changed by this audit","legacy_paths":"PASS - preserved","approved_bundle_v1_1":"PASS - existing approved reference preserved","audit_scope":"PASS - only audit artifacts added"},
}

report = f"""# Runtime V1 Architecture Audit\n\n- Audit version: `RUNTIME_ARCHITECTURE_AUDIT_V1`\n- Date: {inventory['generated_on']}\n- Scope: read-only architecture and wiring audit. No production runtime, prompt, KB, retriever, evidence, citation, or answer changes.\n\n## Executive conclusion\n\nThe current internal runtime is `run_e2e → DenseRetrieverV1 → Evidence Sufficiency V1 → Citation Support V1 → Answer Generation V1`. Router and Dynamic/Hybrid are not wired into it. There is no user-facing CLI/API/chat entrypoint for this chain. The recommended demo is therefore a thin user-facing adapter over the frozen V1 orchestrator, after connecting the already-approved Frozen Bundle V1.1 portability reference.\n\nThe architecture is structurally complete but operationally blocked at the default retriever's bundle integrity verification. Keep Dense V1 as the main retriever for now; Dynamic/Hybrid remains evaluation-only.\n\n## Current actual runtime\n\n```text\nDirect Python caller (no unified user-facing entrypoint)\n  -> src.e2e_orchestrator_v1.run_e2e(query, case_id)\n  -> src.retrieval_v1.DenseRetrieverV1.retrieve\n  -> src.evidence_sufficiency_v1.evaluate_evidence\n  -> src.citation_support_v1.build_support_package\n  -> src.answer_generation_v1.generate_answer\n  -> structured final answer object\n```\n\nThe actual call graph is present in `src/e2e_orchestrator_v1/runtime.py`. The current working-tree execution is blocked before retrieval output when `DenseRetrieverV1._verify_bundle()` sees the Windows raw-file/hash portability mismatch. Existing proxy/shadow results must not be described as actual user runtime.\n\n## Recommended Demo Runtime V1\n\n```text\nDemo CLI/API adapter (new integration task)\n  -> approved Frozen Bundle V1.1 portability-aware retriever loading\n  -> UnifiedE2EOrchestratorV1.run_e2e\n  -> Evidence Sufficiency V1\n  -> Citation Support V1\n  -> Answer Generation V1\n  -> rendered answer + citations + status\n```\n\nRouter is intentionally outside the main path until its contract is approved. Dynamic/Hybrid is a shadow branch only.\n\n## Module status matrix\n\n| Module | Status | Current role | Key finding |\n|---|---|---|---|\n""" + "\n".join(f"| `{m['name']}` | `{m['status']}` | {m['production_role']} | {m['notes']} |" for m in modules) + """\n\nAllowed status vocabulary used: `FROZEN_PRODUCTION_READY`, `PRODUCTION_CANDIDATE`, `EXPERIMENTAL`, `EVALUATION_ONLY`, `LEGACY`, `BLOCKED`, `UNKNOWN`.\n\n## Wiring audit\n\n| Edge | Status | Evidence |\n|---|---|---|\n""" + "\n".join(f"| {w['edge']} | `{w['status']}` | {w['evidence']} |" for w in wiring) + """\n\n## Duplicate and ambiguous runtime inventory\n\n- Router V0/V0.1/V0.2 and web-search routers coexist under experiments; retain as history/experiments, do not import into the current chain.\n- Dense, BM25, Dynamic and Hybrid retrievers coexist; Dense V1 is the current frozen-chain choice, while Dynamic/Hybrid remains evaluation-only.\n- Evidence/Citation/Answer historical versions coexist with `src/*_v1`; only the V1 source runtime is called by the orchestrator.\n- Proxy and actual runtime shadow evaluations are separate evidence classes; neither is a user-facing runtime.\n\n## Hybrid decision\n\n**KEEP_DENSE_V1_FOR_NOW.** Dynamic/Hybrid has no approved production wiring and no gold-regression evidence showing superiority. It is also not connected to the frozen Evidence/Citation/Answer contracts.\n\n## Minimum integration gap\n\n### P0\n\n1. Add one user-facing demo entrypoint that calls `run_e2e`.\n2. Connect the approved Frozen Bundle V1.1 portability-aware loader so the default retriever passes integrity verification.\n\n### P1\n\n1. Run actual Frozen Runtime E2E through the repaired loader.\n2. Define and evaluate a Router→Retriever contract before enabling routing.\n\n### P2\n\n1. Re-evaluate Dynamic/Hybrid against the frozen gold set before any promotion.\n2. Add observability and rendering around the structured result.\n\n## Answer Generation special audit\n\nAnswer Generation V1 consumes the Frozen Citation Support V1 package, uses the local Qwen Qwen2.5 1.5B GGUF adapter when no adapter is injected, and has no external API dependency. The orchestrator enforces status propagation, provenance links, prompt-injection refusal, and no-model-call behavior when support is blocked. Its remaining gap is not a new answer algorithm: it lacks a user-facing caller and is currently upstream-blocked by retriever integrity verification.\n\n## Agent/runtime audit\n\nNo agent loop, tool-use loop, planning layer, or memory subsystem was found in the current frozen chain. Runtime V1 should remain a deterministic orchestrator call; do not introduce agent autonomy in the integration task.\n\n## Frozen integrity\n\n- Core frozen paths: PASS — untouched by this audit.\n- Legacy frozen paths: PASS — preserved.\n- Approved Frozen Bundle V1.1 reference: PASS — preserved.\n- Scope compliance: PASS — only audit/report artifacts are added.\n\n## Next single task\n\n**Runtime V1 Integration**: create the thin demo adapter, route it through the approved V1.1 portability-aware retriever loading, execute real Frozen Runtime E2E, and render the structured answer/citation/status object. This is an integration and validation task, not a KB/retriever-quality redesign or an answer-generation prompt change.\n"""

def main() -> None:
    (OUT / "audit").mkdir(parents=True, exist_ok=True)
    (OUT / "reports").mkdir(parents=True, exist_ok=True)
    (OUT / "audit" / "runtime_architecture_inventory_v1.json").write_text(json.dumps(inventory, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
    (OUT / "reports" / "runtime_architecture_report_v1.md").write_text(report, encoding="utf-8")
    print(OUT / "audit" / "runtime_architecture_inventory_v1.json")
    print(OUT / "reports" / "runtime_architecture_report_v1.md")

if __name__ == "__main__":
    main()
